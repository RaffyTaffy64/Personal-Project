import { Users, Events, PurchasedEvents } from "./model";

const user = await user.create({
    id: ' ',
    firstName: ' ',
    lastName: ' ',
    password: ' ',
    email: ' ',
    address: ' ',
    birthdate: ' '
})

console.log(user)